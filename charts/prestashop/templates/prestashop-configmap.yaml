apiVersion: v1
kind: ConfigMap
metadata:
  name: prestashop-config
data:
  prestashop_database_user: "admin"
  prestashop_database_name: "mariadb-db"
  prestashop_first_name: "Bitnami"
  prestashop_last_name: "User"
