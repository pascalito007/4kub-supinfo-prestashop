kind: PersistentVolume
apiVersion: v1
metadata:
  name: mariadb-pv-volume
  namespace: apps-ns
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path:  /mnt/data
